'use strict';

openstudioApp.factory('Shared', ['$log', function ($log) {
  var service = {};

  //service.startSpinner = function () {
  //  usSpinnerService.spin('spinner');
  //};

  //service.stopSpinner = function () {
  //  usSpinnerService.stop('spinner');
  //};

  return service;
}]);
